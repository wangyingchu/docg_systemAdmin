window.Vaadin.Flow.feature_RelationConceptionEntitiesPairChart = {
    initLazy: function (c) {
        // Check whether the connector was already initialized
        if (c.$connector) {
            return;
        }
        c.$connector = {
            // functions
        };
    }
}